import React from 'react';
import { createStackNavigator } from '@react-navigation/stack';
import { TouchableOpacity, View } from 'react-native';

export const ApprovalsNavigator = () => {
  return (
    <View>
    </View>
  );
};
