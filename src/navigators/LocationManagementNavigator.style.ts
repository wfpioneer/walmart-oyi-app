import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  rightButton: {
    flex: 1,
    width: 55,
    alignItems: 'center',
    justifyContent: 'center'
  },
  image: {
    width: 25,
    height: 25,
    marginRight: 10
  }
});
