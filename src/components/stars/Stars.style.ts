import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rateContainer: {
    flexDirection: 'row'
  },
  container: {
    alignItems: 'center'
  }
});

export default styles;
