export interface ZoneItem {
    zoneId: number,
    zoneName: string,
    aisleCount: number
}

export interface AisleItem {
    aisleId: number,
    aisleName: string,
    sectionCount: number
}

export interface SectionItem {
    sectionId: number,
    sectionName: string,
}
