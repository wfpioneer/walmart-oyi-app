import React from 'react';
import { Text, View } from 'react-native';

export const PrintListsScreen = (): JSX.Element => (
  <View>
    <Text> Print List Screen</Text>
  </View>
);

const PrintLists = (): JSX.Element => (<PrintListsScreen />);

export default PrintLists;
